# Only stable options -- CI runs rustfmt from the stable toolchain
# (rust-toolchain.toml), so nightly-only keys would be silently ignored locally
# and hard-error nowhere useful.
edition = "2021"
max_width = 100
use_small_heuristics = "Default"
newline_style = "Unix"
